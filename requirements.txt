# The Python side of the build and the tests. The C++ side needs only a C++17
# compiler, CMake >= 3.18 and the MuJoCo headers + library that ship INSIDE
# the mujoco wheel below -- nothing to install system-wide.
# Pinned to what microduck-rl's numbers were measured on.
mujoco==3.12.0
numpy==2.2.6
pybind11==3.1.0
pytest
